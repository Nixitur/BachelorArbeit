package example;

/**
 * An example interface to check if tracing and embedding works fine, even though the calling method is declared in an interface.
 * @author Kaspar
 *
 */
public interface ExampleFace {
	void interfaceMethod();
}
